# ============================================================
# myfinance — Python 依赖清单 (requirements.txt)
# 生成自本机 conda 环境 chip_analyzer（2026-07-17）
# 安装：  pip install -r requirements.txt
# 如需放宽版本，把 == 改成 >= 即可
# ============================================================

# ---- 核心：运行 streamlit_demo.py / price_segmenter_v10 / related_stocks / tdx_quant 必需 ----
numpy==2.4.3
pandas==2.3.3
scipy==1.17.1
matplotlib==3.10.8
streamlit==1.59.2
easy-tdx==1.20.3
eltdx==1.0.2
pytdx==1.72
requests==2.32.5

# ---- 在线取数（juejing.py，被 daily_scan / _run_* / _eval_* 等批量脚本调用）----
akshare==1.18.43
efinance==0.5.8
adata==2.9.5
# gm（掘金量化）：需配合掘金终端 + 账号才能运行；仅 juejing.py 用到，主程序不依赖。
# 若只用 streamlit_demo / price_segmenter / related_stocks，可注释掉此行。
# gm==3.0.183

# ---- 仅旧版脚本使用（price_segmenter_v8/v9 及备份文件；主流程已移除 finshare）----
finshare==1.1.0
