# LESSONS.md — 经验教训与防错清单（polo4111，持续更新）

> 本文件记录本项目（黄金坑策略）所有踩过的坑、出错与失败之处，含现象/根因/教训/预防动作。
> **每个 agent / AI（含 reasonix、豆包）开始任何任务前必须先读本文件**，避免重复犯错。
> 新增教训必须追加到对应分类，并标注日期。

---

## 一、未来函数（最高严重度，零容忍红线）

### L1. 放量堆加仓确认 = 未来函数（代价最大）— 2026-08-28
- **现象**：reasonix 报"放量堆确认 20日胜率 97.6%/93.2%、均值+21.9%"，曾作为加仓决策依据。
- **根因**：用「出坑后7天放量堆」信息选股，却从**出坑日(lch)** 算收益——等于提前知道未来 7 天有放量堆。真实可执行（确认后 lch+7 买入持有20日）：胜率仅 **56.8%**、均值+3.42%，**不如不买（82.4%）**。
- **教训**：**"用未来信息选股、从过去时点算收益"是最隐蔽的未来函数**，AI 最容易犯。任何"确认层/增强层"报出 90%+ 胜率，第一反应必须是怀疑未来函数。
- **预防**：① 凡"事后才知道的分组信息"（如未来7天放量堆）绝不能用于选股或从过去时点算收益；② 可执行口径 = 确认信息出现时点之后买入、从买入时点算收益；③ 上截断一致性自检。

### L2. pass2 幽灵信号（重算/回溯机制引入）— 2026-08-28
- **现象**：detect_golden_pit 的 pass2 用"坑前 std"重算坑段时，把坑底回溯到出坑日之前，而确认坑段的 pass1 起点在**出坑日之后**（例 sh600686：坑底394/出坑395，pass1坑段起点404）→ 出坑日无法产生该信号（占 0.8%）。
- **根因**：pass2 扩展段 `[s0-10, e0+10]` + 坑前 std 重算，使信号要素（坑底/出坑日）定义在"确认数据出现"之前。
- **教训**：任何"重算/扩展/回溯"型机制都可能把信号要素推到确认时点之前。
- **预防**：已加因果保护 `if lch is not None and lch < s0: continue`（出坑日不得早于 pass1 坑段起点）。两处：`panic_reversal.detect_golden_pit` + `golden_pit_v2_backtest.detect_golden_pit_v1`。

### L3. tdx 数据时间戳错位 — 2026-08-28
- **现象**：tdx 1000池信号数与胜率异常，部分信号收益失真。
- **根因**：`all_pages` 返回页序混乱 + float 时间戳溢出，导致 K线索引错位（隐式未来/过去错位）。
- **教训**：**外部数据源返回顺序必须显式排序**，时间戳必须用 int64 秒存储。
- **预防**：`get_daily_kline_from_tdx` 已修复（排序 + int64 秒）；新增数据源接口时默认先做排序+类型核验。

### 通用红线
- 实盘信号判定标准 = 信号产生时点用「当日及以前」数据 **100% 复现**（截断一致性测试，脚本 `golden_pit_lookahead_check.py`）。
- **reasonix / 任何 AI 输出的胜率结论，默认先怀疑，必须过截断自检才可采信。**

---

## 二、AI 结论可信度

### L4. reasonix 胜率结论未复现 — 2026-08-28
- **现象**：reasonix 报的"放量堆 97.6%"、"6维评分甜点区 76%/均值20%"在新浪+1206口径下均未复现。
- **根因**：reasonix 的结论可能来自未来函数口径、不同数据源口径或不可复现的样本。
- **教训**：**第三方/AI 结论 = 假设，必须本地方案 + 本地数据复现后才可采信**。
- **预防**：任何新结论（尤其 90%+ 胜率）上线前过两条：① 本地方案复现；② 截断自检。

### L5. 6维评分"无排序能力" — 2026-08-28 定案
- **现象**：v2.0 声称甜点区 76%，本地 1000池实测甜点区 88.1% ≈ 基线 88.4%，无区分度。
- **教训**：评分排序类结论在不同数据源/口径下可能平移失真；**本地数据无排序能力时果断不采用**，不迷信参数调优。
- **预防**：排序类策略先做"甜点区 vs 基线"对比验证，无显著差即放弃。

---

## 三、策略误区（已证伪路径）

### L6. 60日持有弱市崩塌 — 2026-08-27 定案
- 基线 60日 70.9%，弱市 2023:44% / 2026:35%。**持有期固定 20 天**，60日禁用。

### L7. 双响炮（黄瓜蘸酱战法）— 2026-08-28 放弃
- 300只 20日 38.5%；严格筛选后 5日50%/10日46.8%。高赔率低胜率本质，均值靠少数大赚(+267%)。日K级无法量化书中分时强度。**放弃，不再折腾。**

### L8. 其他已证伪：回踩坑(45-53%)、DMA、reg60、主升(可测极限23%)
- 见 skill「已证伪路径清单」。**低胜率/不可测机制一律列入清单，不再投入。**

---

## 四、工程操作

### L9. python heredoc 字符串引号嵌套 SyntaxError — 2026-08-28
- **现象**：用 `python3 - <<'EOF'` 编辑含 `"""` 的文本时，内部双引号导致 `unterminated string literal`。
- **教训**：heredoc 里写含引号的文本，避免 `"""` 嵌套；用单引号包裹外层或拆段替换。
- **预防**：脚本编辑用独立 .py 文件或避免内层三引号。

### L10. 修改前未强制同步 — 2026-08-28 补强
- 本地与 origin/master 不一致就动手改，导致冲突/覆盖。
- **预防**：已写入 AGENTS.md 第2节——**每次修改前先 `git fetch` 对比，落后先 `pull --rebase`，有未提交先 `stash`**。

### L11. skill 版本漂移 — 2026-08-28 修复
- 系统级 `.user_skills/golden-pit-strategy` 长期停留在旧 v1（坑长≥8+6维评分+81.2%旧数据），与仓库 `.reasonix/skills` 不一致，导致加载旧口径。
- **预防**：已定统一管理——仓库 `.reasonix/skills/` 唯一权威，改完跑 `bash sync_skills.sh` 同步系统副本（见 AGENTS.md 第3.5节）。

---

## 五、环境管理

### L12. SSH 密钥随环境重置丢失 → push 失败 — 2026-08-28（多次）
- **现象**：`git push` 报 "repository does not exist"，因 `/home/user/.ssh/id_ed25519` 在环境重置后丢失。
- **教训**：**环境会重置**，密钥、配置、未入库文件都会丢。
- **预防**：① SSH 密钥备份到仓库 `.ssh_backup/`（已入 .gitignore 保护，防私钥误入库但保留本地恢复）；② 推送前先 `test -f ~/.ssh/id_ed25519`，丢失则从 `.ssh_backup` 恢复；③ 一切有价值的产物、结论、skill **必须入库**。

### L13. 本地工作区产物未入库即丢失 — 持续
- 环境重置导致本地缓存、脚本、结论丢失。
- **预防**：回测数据缓存、脚本、skill、关键结论一律入库；运行期垃圾入 `.gitignore`（已配置）。

---

### L14. 数值输出缺乘100的隐蔽显示bug — 2026-08-28
- **现象**：画图脚本 `print(f'口径A: 总收益{finalA/INIT-1:+.1f}%')` 显示 +2.5%，但真实是 +246.1%——`finalA/INIT-1` 是倍数(2.46)，没乘100却加了 `%` 号，格式完全不匹配，几乎骗过所有人。
- **教训**：**倍数(如2.46)与百分比(246%)混用**是量化输出最常见的隐蔽错误；finalA=346万 却显示 +2.5%，数值层面毫无预警。
- **预防**：百分比输出统一 `value/INIT*100-100` 或 `(value/INIT-1)*100`，并在自检时**用最终资产绝对值交叉验证**(346万 ↔ +246% 自洽)，不要只信格式化字符串。

## 快速自查清单（每次任务开始前）
- [ ] 读过本 LESSONS.md？
- [ ] 本地与 origin/master 已同步？
- [ ] 新信号逻辑过 `golden_pit_lookahead_check.py` 截断自检（100%）？
- [ ] 任何 90%+ 胜率结论已用本地方案复现？
- [ ] reasonix/AI 结论默认持怀疑，已过自检？
- [ ] 数据源返回顺序已显式排序、时间戳 int64？
- [ ] 修改 skill 后已跑 `bash sync_skills.sh`？
- [ ] SSH 密钥在位？产物已入库？
